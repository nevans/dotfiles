# other include files can be included by these
# order is important: last one overrides the earlier ones
[include]
  path = ~/.local/share/git/config
  path = ~/.config/git/config

# vim:ft=gitconfig
