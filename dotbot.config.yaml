- defaults:
    link:
      relink: true
      create: true

- clean: ['~', '~/.config']

- create:
    - ~/.config
    - ~/.cache

- link:
    ~/.vim:   vim
    ~/.vimrc: vimrc

- shell:
  - git submodule sync --recursive
  - git submodule update --init --recursive
